/**
 * Store links for the landing page. The app isn't in either store yet, so
 * both are "#" placeholders. Once the real listings exist, drop the https
 * URLs in here — StoreButtons switches to target="_blank" automatically for
 * anything external.
 */
export const siteConfig = {
  appStoreUrl: "#",
  playStoreUrl: "#",
} as const
