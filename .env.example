# Supabase — shared project with the Hello Linden app (jeesrwrmpsutigeedcry).
# Copy to .env.local and fill in. Both are server-only: never add a NEXT_PUBLIC_
# prefix, since the secret key bypasses row level security.
SUPABASE_URL=https://jeesrwrmpsutigeedcry.supabase.co
SUPABASE_SECRET_KEY=
